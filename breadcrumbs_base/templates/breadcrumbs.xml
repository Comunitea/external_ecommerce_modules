<?xml version="1.0" encoding="UTF-8" ?>
<odoo>

    <!-- Breadcrumbs main wrap container -->
    <template id="breadcrumbs_bar" name="Breadcrumbs Bar">
        <div t-att-class="'wp_breadcrumbs_base' if 'portal_my_home' not in xmlid else 'wp_breadcrumbs_base_portal'">
            <div class="container">
                <div class="breadcrumbs_base mb16">
                    <t t-set="breadcrumbs" t-value="website.generate_breadcrumbs(main_object, website)"/>
                    <ol class="breadcrumb" t-if="breadcrumbs">
                        <li>
                            <a href="/"><i class="fa fa-home" alt="Home" rel="noindex,nofollow"/></a>
                            <span>/&amp;nbsp;</span>
                        </li>
                        <t t-foreach="breadcrumbs" t-as="crumb">
                            <li t-att-class="'active' if crumb.active else None">
                                <span t-if="crumb.active" t-esc="crumb.name.capitalize()"/>
                                <a t-else="" t-att-href="crumb.url" t-esc="crumb.name.capitalize()" t-att-alt="crumb.name" rel="noindex,nofollow"/>
                            </li>
                            <span class="breadcrumb-separator" t-if="not crumb.active">&amp;nbsp;/&amp;nbsp;</span>
                        </t>
                    </ol>
                </div>
            </div>
        </div>
    </template>

</odoo>
